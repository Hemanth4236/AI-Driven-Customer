# AI Driven Customer
## Notes

The app reads data from [data/customers.csv](data/customers.csv) and model artifacts from [models/](models/), so those files must stay committed to the repository for the project to work.
